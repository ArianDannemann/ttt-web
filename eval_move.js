/**
 * Used to store a move together with its evaluation score
 */
class Eval_Move
{
    move = 0;
    evaluation = 0;

    constructor(move, evaluation)
    {
        this.move = move;
        this.evaluation = evaluation;
    }
}
